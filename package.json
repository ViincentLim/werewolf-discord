{
  "name": "werewolf-discord",
  "version": "1.0.0",
  "scripts": {
    "build": "tsc --build",
    "start": "node ./dist",
    "build-and-start": "npm run build && npm run start",
    "deploy_commands": "npm run build && node ./dist/deploy_commands.js",
    "game_administrative": "npm run build && node ./dist/game_administrative.js"
  },
  "dependencies": {
    "discord-interactions": "^3.2.0",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "firebase-admin": "^11.4.1",
    "node-fetch": "^2.6.7"
  },
  "devDependencies": {
    "@types/express": "^4.17.15",
    "@types/node-fetch": "^2.6.2"
  }
}
