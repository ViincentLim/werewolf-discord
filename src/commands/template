const HelloCommand: Command = {
    data: {
        name: ??,
        description: ??,
    },
    async execute(interaction, gameState) {
        return {
            content: ??,
        }
    }
}

export default HelloCommand